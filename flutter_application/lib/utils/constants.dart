import 'package:flutter/material.dart';

const fPrimaryColor = Color(0xFF008c83);
const fTextColor = Color(0XFFFFFFFF);
const fAninationDuration = Duration(milliseconds: 200);
const fPointsColor = Color(0xFF96C1AF);
